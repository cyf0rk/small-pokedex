import styled from 'styled-components';

const AppStyled = styled.div`
  background-color: #eeeeed;
  min-height: 100vh;
`;

const PageContainer = styled.div`
  margin: 0 20px;
`;

export { AppStyled, PageContainer };
